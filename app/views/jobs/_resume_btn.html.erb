<% if current_user && current_user.is_member_of?(job) %>
<%= link_to("已申请", '#', class: "btn btn-default btn-xs disabled") %>
<% else %>
<%= link_to("马上申请践行", new_job_resume_path(job), class: "btn btn-success btn-xs") %>
<% end %>
